name = "investing-platform-trigger"
main = "worker.js"
compatibility_date = "2025-01-01"

# Add your GITHUB_TOKEN secret via:
#   wrangler secret put GITHUB_TOKEN
# (it will prompt for the value — do NOT commit the token here)
